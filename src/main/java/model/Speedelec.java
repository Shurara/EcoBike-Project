package model;

public class Speedelec extends ElectroBike {

    public Speedelec(String bikeType, String brand, int maxSpeed, int bikeWeight, boolean isFrontAndBackLight, int batteryCapacity, String color, int price) {
        super(bikeType, brand, maxSpeed, bikeWeight, isFrontAndBackLight, batteryCapacity, color, price);
    }
}
